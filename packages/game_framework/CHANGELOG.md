## 0.1.0

* Initial release: shared framework for turn-based multiplayer games over BLE.
* Abstract `GameEngine` and `GameState` interfaces for any board game.
* `GameSession` manages turn flow and move synchronisation over a BLE connection.
* Reusable `LobbyScreen` and `GameScaffold` widgets.
